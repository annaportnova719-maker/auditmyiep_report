[build]
builder = "nixpacks"
buildCommand = "cd frontend && npm install && npm run build && cd ../backend && python3 -m pip install -r requirements.txt"

[deploy]
startCommand = "cd backend && gunicorn app:app"
healthcheckPath = "/api/health"
